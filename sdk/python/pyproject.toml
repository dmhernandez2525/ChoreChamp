[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chorechamp-sdk"
version = "1.0.0"
description = "Official Python SDK for the ChoreChamp Public API"
requires-python = ">=3.9"
dependencies = ["requests>=2.31.0"]
