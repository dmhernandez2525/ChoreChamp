# ChoreChamp Render Blueprint
# https://render.com/docs/blueprint-spec
#
# Services deployed:
# - chorechamp-db (PostgreSQL free tier)
# - chorechamp-api (Node.js API)
# - chorechamp-site (Static web app)
#
# Note: rootDir is set to repo root to handle pnpm workspaces properly
# Note: NODE_ENV=development is set during install to include devDependencies

services:
  # Web App (Static)
  - type: web
    runtime: static
    name: chorechamp-site
    # Root dir is repo root to support pnpm workspaces
    buildCommand: npm install -g pnpm && NODE_ENV=development pnpm install && pnpm --filter @chorechamp/web build
    staticPublishPath: apps/web/dist
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable

  # API Server
  - type: web
    runtime: node
    name: chorechamp-api
    plan: free
    # Root dir is repo root to support pnpm workspaces
    # NODE_ENV=development during install to get devDependencies (esbuild, etc.)
    buildCommand: npm install -g pnpm && NODE_ENV=development pnpm install && pnpm --filter @chorechamp/api build
    # One-time: Drop tables to allow uuid->text migration, then push schema
    startCommand: node -e "const p=require('postgres');const c=p(process.env.DATABASE_URL);c\`DROP TABLE IF EXISTS notification_log, notification_preferences, device_tokens, coppa_consents, password_reset_tokens, sessions, accounts, user_households, invite_codes, members, chores, chore_completions, households, users CASCADE\`.then(()=>c.end())" && pnpm db:push && cd apps/api && node dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
      - key: DATABASE_URL
        fromDatabase:
          name: chorechamp-db
          property: connectionString
      - key: BETTER_AUTH_SECRET
        sync: false
      - key: BETTER_AUTH_URL
        sync: false
      - key: CORS_ORIGIN
        sync: false

# PostgreSQL Database
databases:
  - name: chorechamp-db
    plan: free
    databaseName: chorechamp
    user: chorechamp
